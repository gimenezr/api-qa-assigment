{
  "name": "api-tests",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "test-local": "mocha test/specs/**/*.js && mocha --reporter json > reports/test-results.json",
    "test": "mocha test/specs/**/*.js && mocha --reporter json > reports/test-results.json",
    "test-report": "mocha test/specs/**/*.js --reporter mochawesome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "chai": "^5.1.1",
    "mocha": "^10.5.1",
    "mochawesome": "^7.1.3",
    "supertest": "^7.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
